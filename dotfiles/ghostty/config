font-family = JetBrainsMono\ NF\ Bold
font-size = 12
background-opacity = 0.9
theme = catppuccin-mocha
# Animated cursor, confirm_os_window_close, cursor_trail, linux_display_server, wheel_scroll_min_lines are not directly supported in ghostty or require different handling.
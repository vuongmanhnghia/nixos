return {
	-- "rest-nvim/rest.nvim",
}
-- :TSInstall http
-- to use http client
-- :Rest run {request name}
-- :Rest run
-- Need to run command in the .http file
-- sample:
--[[ ### home
GET {{url}}/home
accept: application/json ]]
